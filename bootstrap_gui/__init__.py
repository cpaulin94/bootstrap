"""bootstrap_gui — shared UI infrastructure for the Tkinter desktop app.

The legacy sections (Portfolio Builder, Space Explorer, Single Bootstrap)
still live in the top-level ``gui.py`` and import their shared building
blocks (theme, widgets, background-job threading, persistence) from here.
The Life Strategy section is implemented entirely in this package.
"""
